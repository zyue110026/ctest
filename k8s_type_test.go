package ctest

import (
	// "fmt"
	"testing"
	// "k8s.io/apiserver/pkg/authentication/serviceaccount"
	// ctest "k8s.io/kubernetes/test/ctest"
	// fixtures "k8s.io/kubernetes/test/ctest/fixtures"
	// "k8s.io/kubernetes/cmd/kubeadm/app/apis/output"
	// "encoding/json"
	// v1 "k8s.io/api/core/v1"
	// ctestglobals "k8s.io/kubernetes/test/ctestglobals"
)

func TestSpecificTypes(t *testing.T) {
	// fmt.Println("🔍 Testing service accounts with saved fixtures...")
	// // ctestglobalsCopy := ctestglobals.Spec.DeepCopy()
	// // fmt.Printf("ctestglobalsCopy: %+v\n", ctestglobalsCopy)
	// config, nil := GenerateEffectiveConfig(ctestglobals.Containers, ExtendOnly)
	// if nil != nil {
	// 	t.Fatalf("Failed to get matched fixtures: %v", nil)
	// }
	// fmt.Print(config)
	// 1) Load all non-null fixtures
	// all, err := fixtures.LoadFixturesAsJSON("test_fixture.json")
}
